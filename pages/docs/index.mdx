---
title: GJCLibrary Documentation
description: GJCLibrary is an open source Library Management System. It includes observability, analytics, and experimentation features.
faq-tags: [security]
---

import { CopyButton } from "@/components/CopyButton";

# Backend Installation

<div className="mt-7 flex items-center font-semibold text-white bg-black p-4 rounded-lg">
  <pre className="flex-1 m-0">
    <code className="language-bash">
      git clone <span className='text-purple-500'>https://github.com/mviner000/gjc-library-api.git</span>
    </code>
  </pre>
  <CopyButton text="git clone https://github.com/mviner000/gjc-library-api.git" />
</div>

## System Requirements

## Python Installation

1. Download Python
   - Visit [Python's official website](https://www.python.org/downloads/)
   - Download Python 3.11 or later
   - During installation, ensure you check "Add Python to PATH"

2. Verify Installation
   ```bash
   python --version
   # or
   python3 --version
   ```

## Virtual Environment Setup

1. Install virtualenv
   ```bash
   pip install virtualenv
   ```

2. Create a virtual environment
   ```bash
   # Windows
   python -m venv venv

   # Linux/MacOS
   python3 -m venv venv
   ```

3. Activate the virtual environment
   ```bash
   # Windows
   .\venv\Scripts\activate

   # Linux/MacOS
   source venv/bin/activate
   ```

## Django Installation

1. Install Django and required packages
   ```bash
   pip install Django==5.0.4
   ```

## Required Dependencies

Install the following dependencies using pip:

```bash
pip install django-cors-headers==3.14.0
pip install djangorestframework==3.14.0
pip install python-dotenv
pip install django-cloudinary-storage==0.3.0
pip install cloudinary==1.40.0
pip install xhtml2pdf==0.2.16
pip install django-ninja==1.3.0
pip install django-ninja-jwt[crypto]
pip install bcrypt==4.2.0
pip install pycryptodome==3.20.0
pip install mysqlclient==2.2.5
pip install django-encrypted-model-fields==0.6.5
pip install pillow_heif==0.20.0
```

Alternatively, you can install all dependencies at once using the requirements.txt file:

<div className="mt-7 flex items-center font-semibold text-white bg-black p-4 rounded-lg">
```bash
pip install -r requirements.txt
```
</div>

## Database Requirements

- MySQL server (version 8.0 or later recommended)
- Proper MySQL client libraries for mysqlclient package

### MySQL Installation

1. Download and install MySQL Server from [MySQL official website](https://dev.mysql.com/downloads/mysql/)
2. Install MySQL development libraries:

   ```bash
   # Ubuntu/Debian
   sudo apt-get install python3-dev default-libmysqlclient-dev build-essential

   # CentOS/RHEL
   sudo yum install python3-devel mysql-devel

   # macOS (using Homebrew)
   brew install mysql-connector-c
   ```
## Key Features

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
  <div className="feature-card">
    <h3>📡 WebSocket Support</h3>
    <p>Built-in WebSocket functionality for real-time communication.</p>
  </div>
  <div className="feature-card">
    <h3>🔄 Database Migrations</h3>
    <p>Effortless database schema management with migrate and makemigrations tools.</p>
  </div>
  <div className="feature-card">
    <h3>🔒 Security Middlewares</h3>
    <p>Protect your app with clickjack, CSRF, and JWT middlewares.</p>
  </div>
  <div className="feature-card">
    <h3>🔐 HTTPS & SSL</h3>
    <p>Force HTTPS redirection and easy integration with Let's Encrypt and Nginx.</p>
  </div>
  <div className="feature-card">
    <h3>⚙️ Flexible Configuration</h3>
    <p>Easily switch between development and production modes.</p>
  </div>
  <div className="feature-card">
    <h3>👤 Admin Management</h3>
    <p>Built-in admin interface for easy content management.</p>
  </div>
</div>

## Get Started

Ready to build your next web application with Eyygo? Follow our [Quick Start Guide](./Environment%20Variables/Settings.mdx)  to get up and running in minutes!

```go
package main

import "github.com/eyygo"

func LoadSettings() {
	err := godotenv.Load()

	environment := os.Getenv("NODE_ENV")

	dbConfig := shared.DatabaseConfig{
		Engine:   os.Getenv("DB_ENGINE"),
		Name:     os.Getenv("DB_NAME"),
		User:     os.Getenv("DB_USER"),
		Password: os.Getenv("DB_PASSWORD"),
		Host:     os.Getenv("DB_HOST"),
		Port:     os.Getenv("DB_PORT"),
	}

	debug := os.Getenv("DEBUG")

	secretKey := os.Getenv("SECRET_KEY")

	installedApps := createAppPaths([]string{
		"eyygo.admin",
		"eyygo.sessions",
		"eyygo.auth",
		"project_name.app_name",
	})

	AppSettings = SettingsStruct{
		InstalledApps:    installedApps,
		WebSocket:        WebSocketConfig{Port: os.Getenv("WS_PORT")},
		AllowedOrigins:   []string{os.Getenv("ALLOWED_ORIGINS")},
		CertFile:         filepath.Join(os.Getenv("CERT_FILE")),
		KeyFile:          filepath.Join(os.Getenv("KEY_FILE")),
		LogFile:          filepath.Join(os.Getenv("LOG_FILE")),
		Debug:            debug,
		TimeZone:         os.Getenv("TIME_ZONE"),
		Database:         dbConfig,
		Environment:      os.Getenv("ENVIRONMENT"),
		IsDevelopment:    os.Getenv("ENVIRONMENT") == "development",
		CSRF: CSRFConfig{
			Secret:    os.Getenv("CSRF_SECRET"),
			TokenName: os.Getenv("CSRF_TOKEN_NAME"),
			Secure:    os.Getenv("CSRF_SECURE") == "true",
		},
	}
}
```

{/* ## Join the Community

<div className="flex space-x-4">
  <a href="https://github.com/eyygo/eyygo" className="community-button">
    GitHub
  </a>
  <a href="https://discord.gg/eyygo" className="community-button">
    Discord
  </a>
  <a href="https://twitter.com/eyygoframework" className="community-button">
    Twitter
  </a>
</div> */}

y